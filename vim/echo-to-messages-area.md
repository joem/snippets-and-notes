# Echo to the messages area

Echo a message to the normal vim message area:

```vim
echom 'message'
```

Show the previous messages:

```vim
:messages
```

Lists don't work with `echom` so you have to `echo` them instead.


