# Sending Array elements as individual arguments


Reference the array name with a `*` in front of it.

Here's an example:

```ruby
arr=["one","two","tres"]
printf "%s, %s, %s", *arr
```


